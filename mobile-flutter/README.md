# Application terrain Flutter (ISS-007)

```powershell
cd mobile-flutter
flutter pub get
flutter test
flutter run
```

Thème : `lib/theme/tokens.dart` (copie de `docs/design/tokens.dart`). Logo : `assets/brand/aquasensus-logo.png`. Aucun écran de volume. Parcours terrain : signalement, intervention hors ligne (SQ6).
