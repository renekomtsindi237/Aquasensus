# Production ONG/mairie : pas de secrets par défaut, traces minimales (ISS-061).
spring:
  flyway:
    locations: classpath:db/migration
logging:
  level:
    root: WARN
    org.aquasensus: INFO
management:
  endpoint:
    health:
      show-details: never
