spring:
  datasource:
    url: jdbc:h2:mem:aqs;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
  flyway:
    enabled: true

aquasensus:
  jwt:
    secret: test-secret-aquasensus-minimum-32b!
  admin:
    password: ChangeMoi!2026
  interne:
    secret: test-internal-secret
  ussd:
    ttl-seconds: 1
  reset:
    code-fixe: "654321"
  fichiers:
    photos: target/test-photos
